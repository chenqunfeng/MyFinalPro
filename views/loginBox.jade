div.modelBox
    div.loginBox.borderBox
        div.borderBox
            h1.title 欢迎使用CQF
        div.borderBox
            input.username.borderBox(title="用户名" placeholder="用户名" type="text")
        div.borderBox
            input.password.borderBox(title="密码" placeholder="密码" type="password")
        div.borderBox
            button.login.transform.pointer(type="button") 登陆
        div.borderBox
            hr.lineGrandient
            a.register.pointer 免费注册→